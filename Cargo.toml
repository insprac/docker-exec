[package]
name = "docker-exec"
version = "0.1.0"
edition = "2021"

[dependencies]
docker-api = "0.14"
futures = "0.3"
tokio = { version = "1", features = ["time"] }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
